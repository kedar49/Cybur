# Kernel Module for dynamic ARP 

### First Build the Kernel Module:

```bash 
make
```

### install module onto kernel:
```bash
sudo insmod cybur.ko
```

### To remove the module:
```bash
sudo rmmod cybur
```
